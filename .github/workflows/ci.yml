name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # Allow build-and-push.yml to gate image builds on this workflow.
  workflow_call:

# Cancel superseded runs of the same branch — saves CI minutes on rapid pushes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (server)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install server dependencies
        # Fall back to `npm install` if the lockfile is slightly out of sync
        # so a missing transitive resolve never blocks the image build.
        run: npm ci || npm install

      - name: Type check server
        run: npx tsc --noEmit

      - name: Setup Node (client)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install client dependencies
        working-directory: client
        run: npm ci || npm install

      - name: Type check client
        working-directory: client
        run: npx tsc --noEmit --project tsconfig.app.json

      - name: Lint client
        working-directory: client
        # Only lint errors block the build; warnings (legacy any/exhaustive-deps) are tracked but non-fatal.
        run: npx eslint .

  test-and-build:
    name: Test & Build
    runs-on: ubuntu-latest
    needs: lint-and-typecheck
    defaults:
      run:
        working-directory: client

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci || npm install

      - name: Run tests
        run: npm test

      - name: Build
        # Placeholder VITE_API_URL so build doesn't fail when prod var missing in CI.
        env:
          VITE_API_URL: https://placeholder.invalid
          VITE_APP_URL: https://placeholder.invalid
        run: npm run build
